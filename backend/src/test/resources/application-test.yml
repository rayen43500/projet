spring:
  data:
    mongodb:
      uri: mongodb://127.0.0.1:27017/fgm_test

fgm:
  seed:
    enabled: false
  jwt:
    secret: "fgm-test-jwt-secret-must-be-at-least-32-bytes-long"
    issuer: http://localhost:8081
    audience: fgm-frontend
    expiration-hours: 1
